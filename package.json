{
  "name": "rollup-plugin-index",
  "version": "1.0.0",
  "description": "Rollup plugin to inject script and link tags into a HTML template.",
  "main": "index.js",
  "module": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -i src/index.js -f esm -o index.js -e fs,path,parse5",
    "prepare": "npm run build"
  },
  "keywords": [
    "rollup-plugin"
  ],
  "author": "Curtis Maloney",
  "license": "ISC",
  "dependencies": {
    "parse5": "^5.1.0"
  },
  "devDependencies": {
    "rollup": "^1.1.2"
  }
}
